const example = function (a, b, ...rest) {
    console.log(a, b, rest);
}
example('basic', 'rest', 'operator', 'usage');

function calcOrDouble(number, basis =2) {
    console.log(number * basis);
}
calcOrDouble(5);














































